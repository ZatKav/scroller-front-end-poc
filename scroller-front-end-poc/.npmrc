# Make `npm ci`/`npm install` resilient to transient registry network timeouts in CI.
# Without this, a single ETIMEDOUT fetching a tarball fails the whole pipeline step.
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000
